public class MyTriangle {
    public String drawTriangle(){
        // variables
        // primitive vs reference
        // int(32bit), long(64bit), float(32bit), double(64bit), char
        // defined by User
        // class is reference type
        // operators ( << >> | & )
        // 3 statements
        // conditional, iteration, general
        return "*\n**\n***\n";
    }
}
